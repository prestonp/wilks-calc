var calc = require('../calc');
var assert = require('assert');

describe('wilks calculator', function() {
  describe.only('coefficients', function() {
    it('should calc coefficients for males', function() {
      var coeff = calc.wilks(142, 'male', true);
      assert(false);
    });
    it('should calc coefficients for males', function() {
      assert(false);
    });
  });

  describe('ratings', function() {
   it('should calc ratings for males', function() {
     assert(false);
    });
   it('should calc ratings for males', function() {
     assert(false);
   });
  });
});
